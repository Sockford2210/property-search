# Copy this file to .env and set real values for local development.
# .env is git-ignored; .env.example is committed as documentation.

# PostgreSQL superuser password for the local 'db' container.
POSTGRES_PASSWORD=
